# Context Compression Lifecycle

The final page must remain a valid offline study dashboard with search details. The workspace also contains a large reference file so compressed context can demonstrate tool-result clearing.
