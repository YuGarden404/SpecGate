# Role Notes

- planner: sees task, checklist, retrieved context, and constraints.
- implementer: sees the implementation plan and can request ordinary file actions through the normal policy path.
- reviewer: sees final artifact evidence and review notes, but not unnecessary draft state.

Role isolation is context evidence only. It must not bypass WorkspacePolicy, snapshot protection, or HITL review.
