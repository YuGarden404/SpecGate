# Retrieval Context Select

Build a static HTML study dashboard. The page should make the Python LLM Gate requirement visible and include search details for the learning plan.

This case is used to show that the `rag-select` strategy can find relevant local notes before the mock agent writes the final artifact.
